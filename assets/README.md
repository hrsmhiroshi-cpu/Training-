# assets

ロゴ原本をここに置いてください（`logo.tif` / `logo.tiff` / `logo.png` / `logo.jpg`）。

    python3 build/prep_logo.py    # logo.png と logo.meta.json を生成
    node build/generate.js        # 全61枚に挿入して再出力

色・アスペクト比は変更されません。
